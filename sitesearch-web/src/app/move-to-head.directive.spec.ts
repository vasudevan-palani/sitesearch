import { MoveToHeadDirective } from './move-to-head.directive';

describe('MoveToHeadDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveToHeadDirective();
    expect(directive).toBeTruthy();
  });
});
