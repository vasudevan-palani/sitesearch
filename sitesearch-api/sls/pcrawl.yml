handler: handler.pcrawl
package:
   artifact: ./sitesearch-api.zip
events:
      - schedule:
            name: ${opt:stage}-pcrawl-scheduler
            rate: cron(0 */1 * * ? *)
            enabled: true
            input:
                request: {}
