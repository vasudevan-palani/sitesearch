handler: handler.recrawlSetup
package:
   artifact: ./sitesearch-api.zip
events:
      - schedule:
            name: ${opt:stage}-recrawlsetup-scheduler
            rate: cron(0/15 * * * ? *)
            enabled: true
            input:
                request: {}
